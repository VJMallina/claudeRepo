# Netlify Configuration File
# https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  # Publish directory (root of the website)
  publish = "."

  # No build command needed (static site)
  command = ""

# Redirect rules for single page app behavior (optional)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false

# Headers for security and performance
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"

# Cache static assets
[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Pretty URLs (remove .html extension)
[[redirects]]
  from = "/about"
  to = "/about.html"
  status = 200
  force = false

# 404 page (optional - can create custom 404.html)
# [[redirects]]
#   from = "/*"
#   to = "/404.html"
#   status = 404
